import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'staffattendance',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
